"use client";
import { Check, Swatches, BookOpenUser, Bookmark } from "@phosphor-icons/react";

export const SwatchesIcon = () => {
  return <Swatches />;
};

export const CheckIcon = () => {
  return <Check size={30} />;
};

export const BookOpenUserIcon = () => {
  return <BookOpenUser size={40} />;
};

export const BookmarkIcon = () => {
  return <Bookmark size={32} weight="duotone" />;
};
